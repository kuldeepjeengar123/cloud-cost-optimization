[tool.poetry]
package-mode = false

[project]
name = "arrk-docs-agent-aws"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "you@example.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv (>=0.19.0,<1.0.0)",
    "requests (>=2.25.0,<3.0.0)",
    "boto3 (>=1.20.0,<2.0.0)",
    "redis (>=5.0.0,<6.0.0)",
    "mcp (>=1.0.0,<2.0.0)",
    "langgraph>=0.2,<0.3",
    "langchain-core>=0.3,<0.4",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.uv]
package = false
